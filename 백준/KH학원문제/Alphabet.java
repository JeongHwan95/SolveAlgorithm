public class Alphabet {
	char alphabet;
	int length;

	//하나의 객체는 알파벳과 길이(시간)을 저장할 수 있다.
	Alphabet(char alphabet, int length){
		this.alphabet = alphabet;
		this.length = length;
	}//Constructor(char alphabet, int length)
}
